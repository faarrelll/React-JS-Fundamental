import styled from "styled-components";

const PinkBg = styled.div({
  backgroundColor: "pink",
});

const CoralBg = styled.div`
  background-color: coral;
`;

export default {
  PinkBg,
  CoralBg,
};
